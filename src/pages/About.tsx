import React, { FC } from "react";
import Layout from "../components/Layout/Layout";

const About: FC = () => {
  return (
    <Layout>
      <div>About</div>
    </Layout>
  );
};

export default About;
