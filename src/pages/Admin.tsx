import React, { FC } from "react";
import Layout from "../components/Layout/Layout";

const Admin: FC = () => {
  return (
    <Layout>
      <div>Admin</div>
    </Layout>
  );
};

export default Admin;
